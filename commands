git work flow -> working directoty -(add) staging area -(commit) local repository -(push) remote repository
git init, git status (working directory have untracked files)
git add example.txt (add files from wd to staging area)
git commit -m "first commit" (staging area to local repository)
git branch (it can shown current branch)
git branch -M master (create a master branch)
git remote add origin https://github.com/asmitha456/git-commands.git
git push -u origin master
push -> branch -> pull request -> merge
git checkout (jump one branch to another)
git fork used to get a project from another account then you can get code into your repository use git clone
git clone https://github.com/asmitha456/git-commands.git (used remote to local repository)
merge conflicts (git merge origin/master)
git fetch (get data without changes)
git pull (get data with changes)
git branch -d copy1 (first checkout from the branch which you want to delete)
git push origin --delete branch_name (delete the branch from remote repository)


echo "# Terraform" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/asmitha456/Terraform.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/asmitha456/Terraform.git
git branch -M main
git push -u origin main
